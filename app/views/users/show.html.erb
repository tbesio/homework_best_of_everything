<div class="page-header">
  <h1>User #<%= @user.id %></h1>
</div>

<dl class="dl-horizontal">
  <dt>Username</dt>
  <dd><%= @user.username.capitalize %></dd>

  <dt>Favorites</dt>
  <% @favorite_dishes.each do |dish,venue| %>
    <dd><%= dish.name %> (<%= venue.name %>)</dd>
  <% end %>

</dl>
